credentials:
- credential_type:
    kind: ssh
    name: Machine
    type: credential_type
  description: ''
  inputs:
    become_method: '{{ become_method_value }}'
    become_username: '{{ become_username_value }}'
    password: '{{ password_value }}'
    username: '{{ username_value }}'
  name: default
  natural_key:
    credential_type:
      kind: ssh
      name: Machine
      type: credential_type
    name: default
    organization:
      name: Default
      type: organization
    type: credential
  organization:
    name: Default
    type: organization
